{
  "name": "@wise-bloom/domain-types",
  "version": "0.0.0",
  "private": true,
  "description": "Storage-neutral TypeScript domain types mirroring the data model (docs/05-Data/70,72). Consumed by both apps.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "^9.17.0",
    "typescript": "^5.7.2"
  }
}
